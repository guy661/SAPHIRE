'use strict';

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

var request = require('request');
var parser = require('node-feedparser');

var RSS = function () {
    function RSS(props) {
        _classCallCheck(this, RSS);

        this.rssList = props.url;
        if (!this.rssList) {
            throw new Error('URL不能为空!');
        }
        if (typeof this.rssList == 'string') {
            this.rssList = [this.rssList];
        }
        this.onChange = props.onChange || function () {};
        this.onError = props.onError || function () {};
        this.request();
    }

    _createClass(RSS, [{
        key: 'request',
        value: function request() {
            var _this = this;

            var pormiseArr = this.rssList.map(function (item, index) {
                return _this.toPromise(item);
            });
            Promise.all(pormiseArr).then(function (post) {
                _this.onChange(post);
            }).catch(function (error) {
                _this.onError(error);
            });
        }
    }, {
        key: 'toPromise',
        value: function toPromise(item) {
            var promise = new Promise(function (resolve, reject) {
                request(item, function (error, response, body) {
                    if (error) {
                        reject(error);
                    } else {
                        var resp = parser(body, function (error, ret) {
                            if (error) {
                                reject(error);
                            } else {
                                resolve(ret);
                            }
                        });
                    }
                });
            });
            return promise;
        }
    }]);

    return RSS;
}();

;

module.exports = function (props) {
    return new RSS(props);
};