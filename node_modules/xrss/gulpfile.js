const gulp = require('gulp');
const babel = require('gulp-babel');
const rename = require('gulp-rename')

gulp.task('default',()=>{
    gulp.src('lib/src.js')
    .pipe(babel({
        presets: ['env']
    }))
    .pipe(rename('index1.js'))
    .pipe(gulp.dest('.'))
    
})
